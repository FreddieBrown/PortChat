#include "threads.c"
#include <pthread.h>

void* sendMessage(void * arg) {
    //Take port and listen to input from
    // console. 
    // Once input is found, send via port
}

void* receiveMessage(void * arg) {
    // Go into a while loop, listening for messages sent 
    // Print to console
}